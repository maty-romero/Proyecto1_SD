{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Servidor.mainServer",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/Servidor/mainServer.py",
            "console": "integratedTerminal",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}